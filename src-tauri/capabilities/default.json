{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The front page. Only the main window gets access to the app's commands; reader windows load remote sites and get nothing.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
